<%=
  @books && @books.map{
    |book|
      <<-Book
        <div>
          <p>#{book.title}</p>
          <p>#{book.author}</p>
          <p>#{book.checked_out ? 'checked out' : 'availible!'}</p>
        </div>
      Book
  }.map(&:strip).join('')
%>